const Error = ({ tel, setTel }) => {
	setTel('');
	return (
		<div>
			<p>
				We appologize for the inconvenience. It looks like you will not able to
				access the application right now.
			</p>
		</div>
	);
};
export default Error;
